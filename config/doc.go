// Package config defines the parsed and resolved devcontainer.json
// configuration types.
//
// The package separates the wire format (rawConfig, internal) from the
// public ResolvedConfig produced by Resolve. See design/resolved-config.md
// for the rationale and the full type specification.
package config
